---
description: Default pipeline for processing service-proxy logs
processors:
  - rename:
      field: temp.ppbgdi.proxy
      target_field: ppbgdi.app.proxy
      description: Rename temp proxy field
      ignore_missing: true
  - rename:
      field: ppbgdi.app.proxy.downstream.http.request.headers
      target_field: ppbgdi.app.proxy.downstream.http.request.header
      description: Rename deprecated http.request.headers to http.request.header
      ignore_missing: true
  - foreach:
      description: Lowercase all http request headers for easier handling
      field: ppbgdi.app.proxy.downstream.http.request.header
      processor:
        lowercase:
          field: _ingest._key
      ignore_missing: true
  - rename:
      field: ppbgdi.app.proxy.downstream.http.response.headers
      target_field: ppbgdi.app.proxy.downstream.http.response.header
      description: Rename deprecated http.response.headers to http.response.header
      ignore_missing: true
  - foreach:
      description: Lowercase all http response headers for easier handling
      field: ppbgdi.app.proxy.downstream.http.response.header
      processor:
        lowercase:
          field: _ingest._key
      ignore_missing: true

on_failure:
  - set:
      field: event.kind
      value: pipeline_error
  - append:
      field: error.message
      value: >-
        Processor '{{{ _ingest.on_failure_processor_type }}}'
        {{{#_ingest.on_failure_processor_tag}}}with tag '{{{ _ingest.on_failure_processor_tag }}}'
        {{{/_ingest.on_failure_processor_tag}}}failed with message '{{{ _ingest.on_failure_message }}}'
